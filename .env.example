# Environment variables for the March Madness Family Pool

# Vercel KV (Upstash Redis) — optional, falls back to in-memory storage
# Get these from your Vercel project → Storage → KV
KV_REST_API_URL=
KV_REST_API_TOKEN=
